[pytest]
addopts = -m "not integration and not docker and not slow"
markers =
    unit: fast tests that do not require external services
    integration: tests that require external services such as MongoDB
    docker: tests that require Docker Compose
    slow: tests that are intentionally slower than the default suite
